/** Universal /// motif — one markup + class everywhere; never use raw slashes. */
export default function TripleSlash() {
  return (
    <span className="triple-slash" aria-hidden="true">
      ///
    </span>
  );
}
